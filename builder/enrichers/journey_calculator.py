import pandas as pd


def calculate_times(df, config):
    """
    Placeholder.

    Eventually this will calculate the shortest journey
    from every station to the primary hub.
    """

    df["canonical_time_to_hub"] = None

    return df